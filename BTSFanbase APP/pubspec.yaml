name: BTSFanbase_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.10.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Icon standar iOS
  cupertino_icons: ^1.0.8

  # Font Google (Poppins dll)
  google_fonts: ^6.3.3

  # --- VERSI FIREBASE YANG SUDAH DIPERBAIKI (ANTI ERROR) ---
  firebase_core: ^3.1.1
  firebase_auth: ^5.1.1
  cloud_firestore: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Mendaftarkan folder gambar
  assets:
    - assets/images/